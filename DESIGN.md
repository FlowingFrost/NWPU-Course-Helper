# 选课助手 · 完整设计稿

> 版本 v1.0　|　适用范围：本地单人使用　|　学校：西北工业大学（示例数据）

---

## 0. 文档目的

本文档是在与你多轮确认需求后产出的**完整详细设计稿**，涵盖：架构选型、数据模型、核心算法（冲突检测 / 意愿值 / 组合枚举与排序）、界面设计、AI 接口、本地存档、导入导出、实施路线图。

文末第 13 节列出**设计假设与待确认项**，标 `[待确认]` 的项请你在开工前最终确认；其余为我在需求范围内做出的设计决定（标 `[决定]`），如有异议也可修改。

---

## 1. 需求快照（已与你对齐的最终口径）

| 主题 | 结论 |
|---|---|
| 形态 | 本地服务器 + 网页（B/S），本机浏览器访问 |
| 课表 | 周一~周日（7 列）× 每天节次网格，可切换「全部周次 / 指定周次」 |
| 课程三类 | ① 内置课（固定）② 必修选课（必选其一，多候选）③ 非必修（兴趣，选 0~K 门） |
| 选课目标 | 时间/周次不冲突 + 学分硬上限 30 + 尽量多选非必修；无时间偏好，以「候选总体评分」（0-5 浮点）表达 |
| 标记确认选 | 把某门课（必修/非必修）的某一候选标记为「确认要选」，其余候选退出枚举；可逆。确认选 ≠ 选课成功，超容量时仍要投意愿值 |
| 本地存档 | 自动保存到本地文件，避免丢失 |
| AI 接口 | 网页内嵌对话（本期实现）；外部工具/JSON 通道后续再实现 |
| 意愿值 | 总预算 150（可改，软约束）；公式 α=0；超容量才算；四舍五入后若能被 5 整除则 +1 |
| 学分 | 硬上限 30（可改）；超了必不可能选 |
| 排序 | 意愿值 70% + 候选总体评分 30%（评分按学分加权平均，意愿值求和）；结果必须显式展示两者 |
| 数据参考 | WakeUp `.wakeup_schedule` 5 行 JSON 格式，打散成时间段 |

---

## 2. 术语表

| 术语 | 含义 |
|---|---|
| **课程 Course** | 逻辑上的一门课（如「机械原理Ⅱ」），有编号、学分、类别、选课数据 |
| **候选方案 Option** | 一门课的一种「老师×时间×周次」上法；必修课有多个候选、必选其一；内置课只有一个候选 |
| **时间段 Segment** | 被打散的最小单元：`星期 + 起始节 + 连节数 + 周范围 + 地点 + 老师`（对齐 WakeUp 的 `day/startNode/step/startWeek/endWeek/room/teacher`） |
| **内置课 builtin** | 已经选课成功（已抢到、无需再抢）的课；固定不可移动，不投入意愿值 |
| **必修课 required** | 必选、多候选、最终选一个候选 |
| **非必修 elective** | 可选、可多选、最终选 0~K 门 |
| **确认选 confirmed** | 把某候选标记为「确认要选」（≠ 选课成功）；同级候选退出枚举；可取消；计入意愿值、不计入评分 |
| **可行组合** | 一个「每个必修选一个候选 + 每个参与非必修选一个候选」的集合，满足：与内置课无冲突、彼此无冲突、学分 ≤ 30 |
| **待选课程** | 评分聚合口径 = 必修（未确认选）+ 纳入组合的非必修（未确认选）；内置课与「确认选」候选不计入评分 |

---

## 3. 架构与技术选型 `[决定]`

```
┌────────────────────────────────────────────────────────┐
│  浏览器 (React + TS)                                     │
│   ├─ 课表网格 / 单双模式 / 周次过滤 / 结果叠加层           │
│   ├─ 课程与候选管理面板 / 导入导出 / AI 对话面板            │
│   └─ 状态存储：Zustand（内存）+ localStorage（镜像缓存）    │
└───────────────┬────────────────────────────────────────┘
                │ HTTP (fetch)
┌───────────────▼────────────────────────────────────────┐
│  本地服务器 (Node.js + Express)                          │
│   ├─ 静态托管前端构建产物                                  │
│   ├─ GET/PUT /api/schedule, /api/settings（存档）          │
│   ├─ POST /api/chat（AI 对话代理，密钥留服务器端）          │
│   └─ POST /api/command（结构化命令入口，供 AI 与后续外部工具）│
└───────────────┬────────────────────────────────────────┘
                │ 读写
┌───────────────▼────────────────────────────────────────┐
│  data/saves/<id>.json + data/settings.json（自动保存+备份+记忆当前存档）│
└────────────────────────────────────────────────────────┘
```

**选型理由**：
- **前端 React + TypeScript + Vite**：课表状态复杂（单/双/叠加、周次过滤、多候选半透明标注、结果多份），React 组件化最省心；TS 保证数据模型严谨。可选的更简方案：纯静态单文件 + 原生 JS（见 13.6）。
- **后端 Node.js + Express**：与前端同语言，仅承担「静态托管 + 存档读写 + AI 代理」三件小事，最薄。
- **样式**：推荐 Tailwind CSS（半透明/多色/叠加快），也可纯 CSS。
- **状态**：Zustand（轻量）+ `useReducer` 即可，不必引入重型库。
- **运行方式**：一条 `npm run dev` 启动（Vite 前端 + Express 后端并行）。

> AI 对话需要 LLM 后端，采用 **OpenAI 兼容接口**（DeepSeek / OpenAI / Moonshot 等均可，`baseURL + key` 可配置），并内置**无 key 的规则解析器**作为兜底（见 §9）。

---

## 4. 数据模型（核心）

### 4.1 顶层结构（存档文件 `saves/<id>.json`）

```jsonc
{
  "meta": {
    "school": "西北工业大学",
    "term": "大二下",
    "startDate": "2026-03-02",
    "daysPerWeek": 7,        // 周一到周日，固定 7
    "nodesPerDay": 13,       // 每天节数，可配置，默认 13（存档）
    "totalWeeks": 16,        // 学期总周数，可配置，默认 16（存档）
    "creditCap": 30,         // 学分硬上限，可改并存档
    "willingBudget": 150     // 意愿值总预算，软约束，可改并存档
  },
  "nodeTimes": [             // 每节起止时间（可配置；导入时来自 WakeUp 第 2 行）
    { "node": 1, "start": "08:30", "end": "09:15" }
  ],
  "teacherRatings": [        // 评分速查表（可选：按老师名复用默认分，录入时自动填充）
    { "name": "谷文韬", "rating": 4.5 }
  ],
  "courses": [ /* 见 4.2 */ ]
}
```

### 4.2 课程（Course）结构

```jsonc
{
  "id": "crs_0",
  "code": "U03G11004",        // 课程编号：查重主键（同名不同写法靠它判定）
  "name": "机械原理Ⅱ",
  "category": "builtin",      // builtin | required | elective
  "credit": 3.5,              // 学分（浮点）
  "willingOverride": null,    // 手动覆盖意愿值（null=用公式自动算）；「冒险取舍」时用
  "color": "#ffff1744",
  "options": [ /* 见 4.3 */ ]
}
```

### 4.3 候选方案（Option）结构

```jsonc
{
  "id": "opt_0_1",
  "label": "谷文韬/董典彪 班",     // 展示名，可空
  "rating": 4.2,                  // 候选总体评分（0-5 浮点）；展示为「总体评分」
  "selected": false,              // 确认选（确认要选该候选，非选课成功）
  "enrolled": 120,                // m：该候选（教学班）当前已选人数
  "capacity": 100,                // n：该候选（教学班）容量
  "segments": [ /* 见 4.4 */ ]
}
```

### 4.4 时间段（Segment）——对齐 WakeUp「打散」模型

```jsonc
{ "day": 1, "startNode": 1, "step": 2,
  "startWeek": 1, "endWeek": 5, "room": "教西A-104", "teacher": "谷文韬" }
```

- `day`：1=周一 … 7=周日。
- `startNode`：起始节；`step`：连节数（节次区间 `[startNode, startNode+step)`）。
- `startWeek/endWeek`：**连续周区间**（闭区间）。断点/跳周 = 拆成多条 Segment。
- `room`、`teacher`：**段级字段**——因为「不同周不同老师/不同地点」是常见情况（见 §1 示例数据）。

### 4.5 三类课程的规则

| 类别 | options 数 | 参与枚举方式 | 是否计入学分 | 是否计入评分/意愿 |
|---|---|---|---|---|
| builtin 内置 | 恒 1（`selected=true`） | 恒在课表 | 是 | 否 |
| required 必修 | ≥1 | 每门必选 1 个 | 是 | 是 |
| elective 非必修 | ≥1 | 被勾选后按组合枚举，选 0~K 门 | 是 | 是 |

### 4.6 关键设计决定汇总 `[决定]`

1. **编号 `code` 是查重唯一键**：同名但符号/空格不同 → 按 `code` 判定为同一门课。
2. **候选总体评分在候选（Option）级**（字段 `rating`，0-5 浮点）：含义是「对该课程候选项的总体评分」（不再叫「老师评分」）。候选内部若跨多老师（多变场景），取这些老师评分的**简单平均**（无学分维度）。另提供 `teacherRatings` 表做录入时自动填充（可选）。
3. **已选人数/容量在候选级**：容量针对的是「教学班（候选）」，随候选变化。
4. **意愿值**：公式自动计算，`willingOverride` 可手动覆盖（应对「冒险」）。
5. **老师/地点为段级字段**：课程级「老师」= 其所有段老师的并集，仅用于展示。

---

## 5. 意愿值计算

```
输入：m = enrolled（已选人数），n = capacity（容量）
if m <= n:  return 0
raw = sqrt(m - n) * exp(m / n)          // α = 0
v = round(raw)                          // 四舍五入
if v % 5 == 0: v += 1                   // 结果能被 5 整除则 +1
return v
```

- 某组合的**总意愿值** `willing_total = Σ willing(课程)`，求和范围 = 必修（确认选 + 待选）+ 非必修（确认选 + 纳入组合）；**仅内置课（已选课成功）不计**。
- 预算 150 是**软约束**：必修-only 组合即使超 150 也要展示（用户可「冒险」少投某课），但在排序里会因超预算而排后。

---

## 6. 冲突检测

两条 Segment 冲突，当且仅当**同时**满足：

```
s1.day == s2.day
且 节次重叠：max(s1.startNode, s2.startNode) < min(s1.startNode+s1.step, s2.startNode+s2.step)
且 周次重叠：max(s1.startWeek, s2.startWeek) <= min(s1.endWeek, s2.endWeek)
```

- 两个候选冲突 ⇔ 存在任一对 Segment 冲突。
- 一个组合**可行** ⇔ 组合内所有候选两两不冲突，且都不与内置课冲突，且总学分 ≤ 30。
- 由于「跳周」已拆成多条 Segment，周次用**整数闭区间重叠**即可，无需单双周特判。

---

## 7. 组合枚举与排序（核心算法）

### 7.1 输入

- 内置课集合（固定）。
- 必修课集合：每门有 `o_i` 个候选。
- 非必修：用户勾选的参与集合；目标门数 `K`（默认「尽量多」）。

### 7.2 枚举

1. 必修：笛卡尔积 `∏ o_i`，每个必修取一个候选。
2. 非必修：枚举被勾选非必修的**子集**（大小 ≤ K），每个子集内每门再取一个候选。
3. 组合 = 一个必修选择 × 一个非必修选择。
4. 过滤：`可行(组合)`（§6）且总学分 ≤ 30。
5. 若必修-only 学分已 > 30 → 无可行组合，前端醒目提示（学分是硬约束，不可商量）。

### 7.3 评分与排序

```
rating_agg   = Σ(credit_i × rating_i) / Σ credit_i        // 待选课程（未确认选）学分加权平均
rating_score = rating_agg / 5                             // 归一化到 [0,1]
willing_total = Σ willing_i                               // 必修+非必修（确认选 + 待选）意愿值求和
willing_score = clamp(1 - willing_total / 150, 0, 1)      // 越少越好；超预算截到 0
final_score  = 0.30 × rating_score + 0.70 × willing_score // 意愿 70% + 评分 30%
```

- `rating_agg` 只统计「待选课程」（未确认选的必修 + 未确认选的纳入非必修）；内置课与「确认选」候选不计入评分。
- `willing_total` 统计「必修 + 非必修」全体（确认选 + 待选）；**仅内置课（已选课成功）不计入意愿值**。
- 排序：`final_score` 降序；并列时依次 `willing_total` 升序、`rating_agg` 降序、稳定序。
- **展示强制要求**：每个结果卡片/课表旁必须**显式显示** `rating_agg`（保留 2 位）与 `willing_total`（整数）、非必修门数、总学分。

### 7.4 复杂度与剪枝

- 组合数 = `∏ o_i(必修) × Σ_{子集≤K} ∏ o_j(非必修)`。
- 剪枝：冲突/学分在组合展开时尽早剪枝；结果只展示 Top N（默认 20，可配）。
- 现实规模（约 13 门课、每门 1~8 候选）完全可承受；若未来暴增，改为贪心+局部搜索（列为扩展，非本期）。

---

## 8. 界面设计（UI）

### 8.1 布局总览

```
┌──────────────────────────────────────────────────────────────┐
│ 顶栏：学期/周次选择器 [全部周次▾|第 N 周▾]  单课表/双课表切换  [结果叠加层☑] │
├───────────────────────────────┬──────────────────────────────┤
│                               │  右侧面板：                   │
│   课表网格（左）               │  · 课程/候选管理（导入、分类、   │
│   7 列 × N 行节次             │    标记确认选、意愿值/评分编辑） │
│                               │  · AI 对话面板（折叠）          │
│                               │  · 设置（学分上限/预算/节数/周数）│
├───────────────────────────────┴──────────────────────────────┤
│ 结果区（第二行）：可行课表 1..N，按评分排序，每个标注评分+总意愿 │
└──────────────────────────────────────────────────────────────┘
```

### 8.2 课表网格

- 列：周一~周日；行：第 1..N 节（N 可配）。
- 单元格显示：课程名 + 老师 + 周范围标注（如「1-10周」或「1-2,5-8周」）+ 地点（空间允许时）。
- **全部周次模式**：显示所有周的并集；同一 `(星期, 节次)` 有多个课程时**并列平铺**（每个课程一个色块，水平/网格排列，按课程色区分），每个色块标注各自周范围。
- **指定周次模式**：只显示该周活跃的课程。
- **颜色**：课程颜色继承 WakeUp `color`（或自动分配）。

### 8.3 单课表 / 双课表模式

- **单课表模式**：内置课 + 已确认课 = 实心色块；被勾选/聚焦的候选课 = **半透明色块**铺在其所有潜在时间段上（淡文字+虚线边框），点选某半透明块可「标记确认选」。
- **双课表模式**：
  - 左表 = 内置 + 已确认（实心）。
  - 右表 = 所有待选课的全部潜在时间段（半透明）。
  - **两表尺寸/网格/行高/列宽必须完全一致**，滚动同步。

### 8.4 结果叠加层

- 通过顶栏开关或快捷键（如 `R`）切换。
- 叠加层 = 把某个「可行组合」渲染成一张完整课表，盖在第一个课表上（半透明底或切换显示）。
- **多份结果**：放在页面**第二行**，横向滚动展示所有可行课表；**只有一份也展示**。
- 每份结果卡显示：排名、`总体评分`（候选总体评分的学分加权平均）、`总意愿值`、非必修门数、总学分；点击某份 → 顶栏课表预览切换为该份。

### 8.5 课程/候选管理面板

- 列表按「内置 / 必修 / 非必修」分组。
- 必修课：展示其所有候选；每个候选可「标记确认选 / 取消」。
- 非必修课：复选框「参与排课」+ 目标门数 K 输入框（默认「尽量多」）。
- 每门课可编辑：名称、编号、学分、颜色；候选可编辑总体评分（0-5）、已选人数/容量、时间段（增删段）。
- **导入入口**：上传 `.wakeup_schedule` / 本工具 JSON。

### 8.6 设置面板

学分上限（默认 30）、意愿值预算（默认 150）、每天节数（默认 13）、每节起止时间（nodeTimes）、学期总周数（默认 16）、起始日期、评分速查表、DeepSeek API key。

---

## 9. AI 接口设计

### 9.1 目标

让 AI（或用户自己）以自然语言**直观录入/修改课程数据**，且所有操作都落为**结构化命令**，保证确定性、可撤销、可回放。

### 9.2 命令 Schema（结构化命令集合）

AI 与外部工具最终都通过它修改数据（后端暴露 `POST /api/command`）：

```
add_course       {code?, name, category, credit, color?, options?[{label?, rating?, enrolled?, capacity?, segments}]}
add_option       {courseId, label?, rating?, segments:[{day,startNode,step,startWeek,endWeek,room,teacher}]}
set_selected     {optionId, selected:bool}
set_rating       {optionId, rating}   // 候选总体评分
set_enrollment   {optionId, enrolled, capacity}
set_willing      {courseId, willingOverride}
delete_course    {courseId}
update_meta      {creditCap?|willingBudget?|nodesPerDay?|totalWeeks?}
import_schedule  {wakeupFilePath}
```

### 9.3 网页内嵌对话（本期实现）

- 前端对话面板 → `POST /api/chat`（消息 + 当前上下文摘要）→ 后端调 LLM（OpenAI 兼容，`function calling` 绑定上述命令）→ 返回命令执行结果 + 自然语言回复。
- API key 由用户在设置页本地填写，明文存入 `data/settings.json`（暂不加密，仅本机使用）。
- **兜底**：无 key 时启用**规则解析器**，支持常见句式：
  - 「添加课程 机械原理Ⅱ 周三 1-2节 1-16周 张老师 教西A-104 3.5学分」
  - 「把毛概标记为确认选」「给「机械原理Ⅱ-谷文韬班」评 4.5 分」「李强班已选 120 满 100」
- 所有 AI 变更走同一命令通道 → 天然支持撤销与存档。

> LLM 后端：**DeepSeek**（OpenAI 兼容接口），key 在设置页本地填写、明文存档（暂不加密）。未填 key 时自动回退到规则解析器。

---

## 10. 本地存档方案

- 主档：多存档存于 `data/saves/<id>.json`（`default` 为初始存档），当前存档 id 记忆在 `data/settings.json` 的 `currentSaveId`；网页左上角可切换/新建存档。
- 触发：前端每次变更 debounce 后 `PUT /api/schedule`；服务端原子写入（先写临时文件再 rename）。
- 备份：每次成功写入前，把旧档复制为 `data/backup/schedule.2026-08-14T19-05-00.json`，保留最近 N 份（如 20）。
- 前端 `localStorage` 镜像，网络断开/刷新不丢；启动时 `GET /api/schedule` 拉取。
- **外部自动同步**：前端每 2 秒轮询 `GET /api/schedule`，检测到外部工具/AI 直接改存档文件后自动刷新（有本地未保存修改时跳过，避免覆盖正在编辑的内容）。
- 手动「导出/导入」JSON 备份按钮（额外的备份手段，非主存档方式）。

---

## 11. 导入 / 导出（WakeUp 格式映射）

`.wakeup_schedule` 5 行 → 内部模型：

| WakeUp 行 | 内容 | 映射 |
|---|---|---|
| 1 | `{courseLen, id, name, ...}` | 节次时长（参考，不强制） |
| 2 | `[{node, startTime, endTime}]` | `meta.nodeTimes` |
| 3 | `{tableName, school, maxWeek, nodes, startDate, ...}` | `meta.term/school/totalWeeks/nodesPerDay/startDate` |
| 4 | `[{id, courseName, color, credit, note}]` | `Course`（名称/颜色/学分；**credit 在示例里是 0，需补**） |
| 5 | `[{id, day, startNode, step, startWeek, endWeek, room, teacher}]` | 按 `id` 归并成 `Course.options[0].segments` |

**导入语义 `[决定]`**：WakeUp 是「已排好的课表」，故导入后默认全部为 **builtin**（1 个候选、`selected=true`）；用户/AI 随后可「降级/复制」为必修或非必修候选，并补充编号、学分、评分、已选/容量、意愿值等 WakeUp 缺失字段。

**导出**：支持导出本工具 JSON；可选「导出为 `.wakeup_schedule`」以回流 WakeUp（扩展项，非本期必需）。

---

## 12. 实施路线图

| 阶段 | 内容 | 产出 |
|---|---|---|
| P1 骨架 | 项目初始化（Vite+React+TS / Express），数据模型与存档读写 | 可启动空课表 |
| P2 课表渲染 | 7×N 网格、周次过滤（全部/指定）、WakeUp 导入、课程/候选管理 | 能看课、能导入 |
| P3 选课逻辑 | 冲突检测、意愿值公式、组合枚举与 70/30 排序、结果第二行 | 能算出可行组合并排序 |
| P4 交互 | 单/双课表、半透明候选标注、标记确认选（可逆）、结果叠加层 | 完整交互闭环 |
| P5 AI | 命令 Schema、`/api/command`、内嵌对话 + 规则解析器（+可选 LLM） | AI 可录入 |
| P6 打磨 | 备份、设置、导出、边界提示（学分超限/必修-only 超预算） | 可用 v1.0 |

---

## 13. 已确认决策（最终口径）

以下为本轮确认后的最终决策，已同步回正文：

1. **每天节数 = 13**，可配置并存档；每节起止时间（`nodeTimes`）同样可配置。
2. **学期总周数 = 16**，可配置并存档（文件 `maxWeek=20` 为 WakeUp 默认，不采用）。
3. **「非必修目标门数 K」**：确认是「非必修」门数（原「必修课数目」为笔误）。
4. **AI 后端 = DeepSeek**（OpenAI 兼容），key 在设置页本地填写、明文存档（暂不加密）；未填 key 时回退规则解析器。
5. **意愿值归一化**：按预算 150 归一（`1 - total/150`，截断到 0）。
6. 架构保留**薄 Node 后端**（静态托管 + 存档 + AI 代理），不采用纯静态方案。
7. **候选总体评分**在候选级（字段 `rating`，0-5 浮点，替代「老师评分」）、已选/容量在候选级、老师/地点在段级（§4.6）。
8. 评分聚合只统计「仍待选择」的课程；**意愿聚合统计「必修 + 非必修」全体（确认选 + 待选），仅内置课（已选课成功）不计入**。
9. 「尽量多选非必修」= 枚举大小 ≤ K 的子集（K 默认不限），按 70/30 评分排序；数量优先可作为后续开关。
10. 结果默认展示 Top 20，可按需调。
