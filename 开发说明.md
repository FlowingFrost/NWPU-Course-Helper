# 选课助手 · 开发说明

本地运行的大学生选课 / 排课助手：网页端 + 浏览器插件。本文是功能与开发说明；**如何启动见 [README.md](README.md)**。

---

## 目录

- [1. 功能总览](#1-功能总览)
- [2. 技术栈与架构](#2-技术栈与架构)
- [3. 开发脚本](#3-开发脚本)
- [4. 目录结构](#4-目录结构)
- [5. 数据模型与核心术语](#5-数据模型与核心术语)
- [6. 意愿值与排序算法](#6-意愿值与排序算法)
- [7. 网页端功能说明](#7-网页端功能说明)
- [8. 浏览器插件说明](#8-浏览器插件说明)
- [9. AI 与外部工具接口](#9-ai-与外部工具接口)
- [10. 数据与存档](#10-数据与存档)
- [11. 常见问题](#11-常见问题)

---

## 1. 功能总览

### 网页端

| 能力 | 说明 |
|---|---|
| 课表渲染 | 周一~周日 × 每天 N 节（默认 13）；「全部周次 / 指定周」切换 |
| 三类课程 | ① 内置课（已选课成功，固定）② 必修课（多候选，必选其一）③ 非必修课（可多选） |
| 确认选 | 把必修/非必修某候选标记为「确认要选」，同级候选退出枚举，可逆 |
| 冲突检测 | 按「星期 + 节次 + 周范围」三重重叠判定，跳周/断点已打散 |
| 组合枚举 | 必修候选 × 非必修子集，过滤冲突与学分超限，输出可行组合 |
| 意愿值 | 超容量才产生意愿值（软预算默认 150）；内置课不计，确认选与待选都计 |
| 排序 | 意愿值 70% + 候选总体评分（学分加权）30% |
| 双课表 | 左表=固定课，右表=待选潜在时间段，尺寸/滚动同步，可淡化/过滤冲突 |
| 结果叠加 | 结果区 Top 20 份可行课表，点击叠加到主课表 |
| 存档 | 多存档切换/新建/删除，自动保存 + 备份 |
| 导入导出 | 导入 WakeUp `.wakeup_schedule` / JSON；导出 JSON 存档 |
| 粘贴解析 | 粘贴教务文本或自然语言指令，规则解析器自动录入 |

### 浏览器插件

- 在教务系统「全校开课查询」页内嵌按钮，**不接触账号密码**，复用当前登录会话。
- 勾选多行批量导入、单课加入候选、悬停预览迷你课表、按课程代码静默查询（一次导入该课全部教学班，含已选人数/容量/是否开放）。

---

## 2. 技术栈与架构

```
┌────────────────────────────────────────────────┐
│  浏览器（React + TS + Vite）                      │
│   课表 / 课程候选管理 / 结果区 / 设置 / 粘贴解析     │
└────────────────┬───────────────────────────────┘
                 │ HTTP (fetch)，/api 代理到 3001
┌────────────────▼───────────────────────────────┐
│  本地服务器（Node.js + Express，server/index.ts） │
│   静态托管 dist / GET·PUT /api/schedule / settings│
│   /api/saves / /api/command / /api/parse         │
└────────────────┬───────────────────────────────┘
                 │ 读写
┌────────────────▼───────────────────────────────┐
│  data/saves/<id>.json + data/settings.json       │
│  + data/backup/（自动备份）                       │
└────────────────────────────────────────────────┘
```

- 前端 React + TypeScript + Vite；后端 Express（薄，只做静态托管 + 存档 + 命令/解析）。
- 选型理由与完整设计见 [`DESIGN.md`](DESIGN.md)。

---

## 3. 开发脚本

| 命令 | 作用 |
|---|---|
| `npm run dev` | Vite 前端（5173）+ tsx 后端（3001）并行，热更新 |
| `npm run build` | `tsc --noEmit && vite build`，产物 `dist/` |
| `npm start` | 仅起后端，托管 `dist/` 与 API |
| `npm run preview` | 预览构建产物 |
| `npm run test:ext` | 插件核心逻辑单测（`tsx --test extension/test/*.test.ts`） |
| `npm run build:ext` | esbuild 打包插件到 `extension/dist/` |
| `npm run pack` | 一键打包成「当前系统」可执行文件（脚本 `scripts/pack.mjs`） |
| `npm run pack:win` | 一键打包成 `windows-amd64` 可执行文件 |
| `npm run pack:linux` | 一键打包成 `linux-amd64` 可执行文件 |
| `npm run pack:src` | 一键打包「源码版」（脚本 `scripts/pack-src.mjs`，供有 Node 的人自建） |

### 打包发布（生成可执行文件 + 插件）

```bash
npm run pack:win        # 产出 releases/windows-amd64-v<版本>/ 及其 .zip
npm run pack:win -- --version v1.0alpha   # 自定义版本号
```

脚本 `scripts/pack.mjs` 会自动完成：`vite build` 前端 → `build:ext` 插件 → esbuild 编译后端 → pkg 打成 `CourseHelper.exe` → 组装到 `releases/<平台>-<架构>-<版本>/`（含 `dist/` 网页、`extension/` 插件、`README.txt`）→ 压缩成 zip。

要点：

- **目标平台**：`pack:win`/`pack:linux` 用 pkg 的 `--target` 指定；在 Linux 上打 Windows exe 属于交叉编译，pkg 首次会联网下载对应平台的 Node 基础二进制。
- **可执行文件运行时**：在**自己所在目录**找 `dist/`（网页）并在同目录生成 `data/`（存档），所以发布时 `CourseHelper.exe` 与 `dist/` 要放一起。
- **插件**：`extension/` 目录直接是「未打包插件」，用 Chrome「加载已解压的扩展程序」载入即可，与 exe 放哪无关。

### 打包源码版（给有 Node.js 的人）

```bash
npm run pack:src        # 产出 releases/src-v<版本>/ 及其 .zip
```

脚本 `scripts/pack-src.mjs` 把项目源码（自动排除 `node_modules/`、`dist/`、`extension/dist/`、`data/`、`releases/`、`.pack/` 等产物与个人数据）打包成 zip。对方解压后按 **`README.md`** 操作即可：

```bash
npm install
npm run dev            # 打开 http://localhost:5173
npm run build:ext      # （可选）构建浏览器插件
```

---

## 4. 目录结构

```
CourseHelper/
  src/                    前端 React + TS
    components/           课表 / 课程面板 / 结果区 / 设置 / 存档 / 解析
    lib/                  算法（冲突/意愿值/枚举排序）、布局、WakeUp 导入、配色
    hooks/                状态与自动保存
  server/                 Express 后端（API + 静态托管）
  shared/                 前后端共享的数据模型 / 命令 / 变更 / 解析器
  extension/              Chrome/Edge 插件（教务捕获）
  docs/                   AI接口协议 / 插件集成设计 / 课程文本格式说明
  DESIGN.md               完整设计稿
  README.md               如何启动
  开发说明.md              本文档
```

---

## 5. 数据模型与核心术语

| 术语 | 含义 |
|---|---|
| **课程 Course** | 逻辑上一门课（编号、学分、类别、多个候选） |
| **候选 Option** | 一门课的某一种「老师 × 时间 × 周次 × 地点」上法 |
| **内置课 builtin** | 已经选课成功（已抢到）的课，固定不可改，**不投入意愿值** |
| **必修课 required** | 多候选、最终选一个 |
| **非必修 elective** | 可选 0~K 门，勾选「参与排课」后进入枚举 |
| **确认选 confirmed** | 把某候选标记为「确认要选」（≠ 选课成功），同级候选退出枚举，可取消；超容量仍计意愿值 |
| **时间段 Segment** | `星期 + 起始节 + 连节数 + 周范围 + 地点 + 老师` |

字段细节见 [`DESIGN.md`](DESIGN.md) §4；AI 视角的字段约定见 [`docs/AI接口协议.md`](docs/AI接口协议.md)。

---

## 6. 意愿值与排序算法

### 意愿值公式（`src/lib/algo.ts`）

```
m = 已选人数，n = 容量
m ≤ n        → 意愿值 = 0          （未满员，无需抢）
m > n        → round(√(m−n) · e^(m/n))，结果能被 5 整除则 +1
```

- 总意愿值 = 必修 + 非必修（确认选 + 待选）意愿值求和；**仅内置课不计**。
- 意愿预算默认 150，软约束：超预算仍展示，排序靠后。

### 评分与排序

```
rating_agg    = Σ(学分 × 候选评分) / Σ学分        // 待选课程，学分加权
rating_score  = rating_agg / 5
willing_score = clamp(1 − 总意愿值 / 150, 0, 1)   // 越少越好
final_score   = 0.30 × rating_score + 0.70 × willing_score
```

排序 `final_score` 降序；并列时依次：总意愿值升序 → 总体评分降序 → 非必修门数降序。

---

## 7. 网页端功能说明

### 7.1 界面布局

```
┌────────────────────────────────────────────────────────────┐
│ 顶栏：存档切换 · 周次选择 · 单/双课表 · 显示开关 · 设置 · 解析 │
├───────────────────────────────┬────────────────────────────┤
│                               │  课程与候选面板（右下区）       │
│        课表网格（左/上）        │  · 课程/候选管理               │
│                               │  · 确认选/评分/人数容量/时间段  │
├───────────────────────────────┴────────────────────────────┤
│ 结果区：可行课表 1..N，标注 总体评分 + 总意愿值 + 学分 + 非必修门数 │
└────────────────────────────────────────────────────────────┘
```

### 7.2 顶栏

| 控件 | 说明 |
|---|---|
| 存档选择器 | 切换 / 新建 / 删除存档 |
| 周次选择器 | 「全部周次」或「第 N 周」过滤 |
| 单/双课表 | 快捷键 `D` |
| 显示课程信息 | 课程块上显示老师 / 周范围 / 地点 |
| 显示已选人数/容量 | 课程块上显示 `已选 n/容量` |
| 显示非必修 | 是否在课表显示非必修 |
| 过滤冲突课程 | （双课表）右侧隐藏与固定课冲突的候选 |
| 结果叠加层 | 快捷键 `R`，把选中结果叠到主课表 |
| 设置 | 打开设置面板 |
| 解析 | 粘贴文本 / 指令 |

### 7.3 课表（单 / 双）

- 单课表：固定课（内置 + 已确认）实心；待选候选半透明虚线块，点击定位到右侧卡片。
- 双课表：左=固定课，右=待选潜在时间段；两表尺寸一致滚动同步，中间分隔条可拖动；点右表候选可淡化其它课程并打开对应卡片。
- 同格子多课程并排平铺，按课程颜色区分，各标周范围。

### 7.4 课程与候选管理

按「内置 / 必修 / 非必修」分组；标签页可筛选，另有「显示已确认」「显示不参与排课」。

课程级字段：名称、课程编号（查重/插件匹配主键）、学分、类别、串联组（同组只选其一）、参与排课。

候选级字段：教学班编号、开放选课、**确认选**、评分（0~5）、已选人数/容量、时间段（星期/起止节/起止周/老师/地点）、锁定（防止粘贴/解析覆盖）。

### 7.5 生成选课结果

1. 设置「非必修目标门数」（留空 = 不限）。
2. 点「生成可行课表」。
3. 结果区横向展示 Top 20 份，每份含排名、总体评分（保留 2 位）、总意愿值（超预算标红）、非必修门数、学分，附迷你课表。
4. 点某份 → 结果叠加层预览（`R` 开关）。

无解时给出诊断：学分超限 / 固定课冲突 / 必修被堵死 / 必修两两无法同时安排。

### 7.6 设置

显示偏好（星期组合区分、行高、均分卡片宽度、淡化其它课程）、存档设置（学期/学校/开学日期/总周数/**学分上限**/**意愿预算**/每天节数）、课表分割线、节次时间、导入 WakeUp、导出/导入 JSON 存档。

### 7.7 粘贴解析（规则解析器，无需 API key）

顶栏「解析」输入框 → `POST /api/parse`。支持：

- 粘贴教务「课程信息表格」网页文本（自动识别课程/编号/学分/时间段）。
- `添加课程 数据结构 必修 3学分`
- `删除课程 数据结构`
- `把数据结构设为非必修`
- `李强班 已选 120 满 100`
- `给机械原理Ⅱ-谷文韬班 评 4.5 分`

### 7.8 存档

顶栏可新建/切换/删除；前端修改 500ms 防抖后自动 `PUT /api/schedule`，写前旧档备份到 `data/backup/`；每 2 秒轮询，外部改档后自动刷新。

---

## 8. 浏览器插件说明

插件把教务「全校开课查询」页的课程（编号、教学班、时间地点、已选人数/容量、是否开放）一键导入本地。不读不存不转发账号密码/Cookie。

### 8.1 构建与加载

```bash
npm run build:ext
```

Chrome `chrome://extensions` → 开发者模式 → 加载已解压的扩展程序 → 选 `extension/dist/`。先启动网页端服务（见 README）。

> WSL 场景：Windows 浏览器访问 `http://localhost:3001` 应能看到页面；打不开则改用 WSL IP，并在插件「设置」里改服务地址。

### 8.2 功能

| 功能 | 操作 | 说明 |
|---|---|---|
| 批量导入 | 勾选多行 → 选导入类型（非必修/必修/内置）→「导入选中」 | 一次导入勾选课程 |
| 加入候选 | 课程名右侧「加入候选」 | 该教学班作为非必修一键导入 |
| 预览 | 悬停「预览」 | 迷你课表：橙色=当前课，实心=内置，半透明=其它候选；与内置课冲突标 ⚠ |
| 静默查询 | 课程代码框填编号 →「静默查询」 | 该课全部教学班一次导入（含人数/容量/开放） |
| 弹窗 | 点工具栏图标 | 服务在线状态 + 打开全屏页 |
| 设置 | 插件「设置」 | 服务地址、默认类别 |

### 8.3 权限

`storage`、`tabs`；`host_permissions` 仅 `localhost:3001` / `127.0.0.1:3001` / `localhost:5173` / `jwxt.nwpu.edu.cn`。静默查询在教务页同源发起，凭证不出浏览器，唯一外发目标是本机 3001。

### 8.4 故障排查

| 现象 | 排查 |
|---|---|
| 「导入失败：…服务是否已启动」 | 服务没起，`npm start` 后重试 |
| 弹窗「未启动」 | 同上，并检查插件设置里的服务地址 |
| localhost 不通 | 改用 WSL IP |
| 「未找到课程行」 | 先点教务页「查询」出结果 |
| 静默查询「未找到学期选择器」 | 换到 `/student/for-std/lesson-search` 标准页 |
| 静默查询「未查到该课程」 | 课程代码错或该学期无此课 |

---

## 9. AI 与外部工具接口

详见 [`docs/AI接口协议.md`](docs/AI接口协议.md)：

1. **HTTP 命令协议**：`POST http://localhost:3001/api/command`，结构化命令（`add_course` / `add_option` / `set_selected` / `set_rating` / `set_enrollment` / `set_willing` / `update_course` / `delete_course` / `update_meta`）。
2. **直接改存档文件**：编辑 `data/saves/<id>.json`，前端每 2 秒自动刷新。
3. **规则解析**：`POST /api/parse`（网页「解析」即此端点）。

> 注：DeepSeek 对话接口目前未接入，`data/settings.json` 中的 `deepseek*` 字段为预留；日常录入走规则解析器。

---

## 10. 数据与存档

```
data/
  settings.json          当前存档 id、DeepSeek 配置（预留）
  saves/<id>.json        主存档（default 为初始存档）
  backup/                每次写入前的自动备份（保留最近多份）
```

- 存档为完整 JSON：`meta`（学校/学期/周数/节数/学分上限/意愿预算）、`nodeTimes`、`teacherRatings`、`courses`。
- 导入 WakeUp：`.wakeup_schedule`（5 行 JSON），导入后默认全部「内置」（已排好的课表），可再降级/复制为必修或非必修并补字段。
- 手动备份：设置里导出 JSON 存档；恢复：导入 JSON 存档。

---

## 11. 常见问题

**Q：改了代码/重新构建后界面还是旧的？**
浏览器缓存了旧 JS。强制刷新 `Ctrl+Shift+R`（Mac `Cmd+Shift+R`），或开无痕。开发模式确认访问 `http://localhost:5173`。

**Q：所有结果「总意愿值」都是 0？**
意愿值只在 `已选人数 > 容量` 时非 0。课程都未满员、或超容量课程被设成「内置/不参与排课」时为 0；确认选的超容量课程会计入。

**Q：学分超限没结果？**
学分是硬约束：必修 + 内置/确认选的最低学分超上限必无解，结果区会诊断。

**Q：怎么固定某候选？**
勾选「确认选」，其余候选退出枚举；取消即恢复。

**Q：候选在粘贴/解析时被覆盖？**
该候选被「锁定」了（`🔒 已锁定`），点解锁后才允许覆盖，用于保护手动协调的时间段。
